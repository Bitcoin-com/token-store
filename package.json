{
  "name": "token-store",
  "version": "0.1.0",
  "description": "store access tokens for command-line authentication",
  "main": "index.js",
  "author": "sethvincent",
  "license": "ISC",
  "scripts": {
    "deps": "dependency-check . && dependency-check . --unused --no-dev && ncu",
    "lint": "standard",
    "test:no-lint": "node tests/*.js | tap-spec",
    "test": "npm run lint && npm run test:no-lint"
  },
  "engines": {
    "node": ">=8.11.0"
  },
  "repository": {
    "type": "git",
    "url": "git@github.com:Bitcoin-com/token-store.git"
  },
  "devDependencies": {
    "standard": "^8.6.0",
    "tap-spec": "^4.1.1",
    "tape": "^4.6.3"
  },
  "dependencies": {
    "configuration-store": "^1.0.1",
    "store": "^1.3.20"
  }
}
